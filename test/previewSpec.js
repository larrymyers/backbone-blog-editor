describe('MarkdownPreview', function() {
    it('Should be initialized with a model containing title and content.', function() {
        var view = new MarkdownPreview({ content: 'Test' });
    });
});